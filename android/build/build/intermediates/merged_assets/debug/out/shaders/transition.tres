[gd_resource type="Shader" format=2]

[resource]
code = "shader_type canvas_item;

uniform float FadeAmount: hint_range(0.0, 1.0) = 1.0;

void fragment(){
    vec4 color = vec4(0.0, 0.0, 0.0, FadeAmount);
	
	COLOR = color;
}"
